# Data directory

Only synthetic sample records belong in this directory. Real scraped datasets, user resumes, application documents, database files, and downloaded company assets must never be committed.

The sample CSV demonstrates the importer schema without redistributing third-party job content.
